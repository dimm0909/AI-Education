# Артефакты BikeFlow

После обучения (`python -m src.train`) создаются:

- `model.joblib` — сериализованная финальная модель + метаданные;
- `metrics.json` — метрики baseline/improved и выбранная финальная модель.

Эти файлы используются сервисом для инференса.
